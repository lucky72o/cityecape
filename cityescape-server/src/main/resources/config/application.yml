liquibase:
    enabled: false
    drop-first: false
    change-log: classpath:database/com.cityescape-master.xml
    check-change-log-location: true

spring:
  data:
    mongodb:
      database: uk-cities-db
  freemarker:
      cache: false


server:
  port: 8090
  context-path: /cityescape

---

spring:
  profiles: 'local'

  datasource:
    url: jdbc:mysql://localhost:3306/city_escape
    username: cityescape
    password: cityescape